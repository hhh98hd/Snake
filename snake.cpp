#include <bits/stdc++.h>
#include <windows.h>
#include <conio.h>

#include "KeyHandler.h"

using namespace std;

int main()
{    
    while (true)
    {
        char c = _getch();
        cout << c << " is pressed" << endl;
        Sleep(1);
    }
}